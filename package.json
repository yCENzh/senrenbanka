{
  "name": "senrenbanka",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd client/serenbanka && npm run dev",
    "build": "cd client/serenbanka && npm run build",
    "deploy": "cd client/serenbanka && npm run build && cd ../.. && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
