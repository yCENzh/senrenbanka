{
  "name": "senrenbanka",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter serenbanka dev",
    "build": "pnpm --filter serenbanka build",
    "deploy": "pnpm build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
