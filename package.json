{
  "name": "senrenbanka",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter serenbanka dev",
    "build": "pnpm --filter serenbanka build",
    "deploy": "pnpm build && npx wrangler deploy --config client/serenbanka/dist/senrenbanka/wrangler.json"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
